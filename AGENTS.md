# Agent Instructions (opencode)

- This repo is an `nvf` (Nix Neovim Framework) configuration.
- To enter a development environment with the helper CLI tools available, run:
  - `nix shell .`
- Within that shell you can print the generated Neovim Lua config with:
  - `nvf-print-config`
